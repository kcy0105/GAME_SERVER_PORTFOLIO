#pragma once

#pragma region COMMON
enum OBJECT_TYPE
{
	OBJECT_TYPE_PLAYER,
};

enum DIR
{
	DIR_UP,
	DIR_DOWN,
	DIR_LEFT,
	DIR_RIGHT
};

enum PLAYER_STATE
{
	PLAYER_STATE_NORMAL,
	PLAYER_STATE_TRAPPED,
	PLAYER_STATE_DEAD,
	PLAYER_STATE_ESCAPE,
};

enum MOVE_STATE
{
	MOVE_STATE_IDLE,
	MOVE_STATE_MOVE,
};

enum MAP_OBJECT_TYPE
{
	MAP_OBJECT_TYPE_NONE,
	MAP_OBJECT_TYPE_BREAKABLE_BLOCK,
	MAP_OBJECT_TYPE_SOLID_BLOCK,
	MAP_OBJECT_TYPE_WATER_BOMB,
	MAP_OBJECT_TYPE_ITEM,
};

enum ITEM_TYPE
{
	ITEM_TYPE_BUBBLE,
	ITEM_TYPE_FLUID,
	ITEM_TYPE_ROLLER,
	ITEM_TYPE_NEEDLE,
};
#pragma endregion

enum class SceneType
{
	None,
	GameScene,
	DevScene,
	EditScene,
};

enum class ColliderType
{
	Box,
	Circle,
};